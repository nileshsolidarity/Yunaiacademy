[build]
builder = "nixpacks"

[build.nixpacks]
pkgs = ["nodejs_20", "pnpm"]

[deploy]
startCommand = "cd apps/api && npx prisma db push --skip-generate && pnpm start"
healthcheckPath = "/api/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
